# TODO List : java-calculator-precouse

## 1. 기본 구분자를 기준으로 숫자 분리하여 합하는 기능
1. 문자열 입력받기
2. 기본 구분자로 숫자 분리하기
3. 분리한 숫자 합하여 결과 반환하기
4. 잘못된 입력 값 예외 처리 - 숫자 외의 문자열은 예외 처리

## 2. 커스텀 구분자를 기준으로 숫자 분리하여 합하는 기능
1. 커스텀 구분자 추출하기
2. 기본, 커스텀 문자로 숫자 분리하기
3. 1.3의 기능을 활용해 결과 반환하기
4. 잘못된 입력 값 예외 처리 -  커스텀 구분자를 잘못 표현한 경우 예외처리 

## 3. 테스트
1. 기본 구분자로 숫자를 분리하여 정확한 결과값이 나왔는가?
2. 커스텀 구분자로 숫자를 분리하여 정확한 결과값이 나왔는가?
3. 빈 문자열의 경우 0의 값을 반환하는가?
4. 잘못된 입력 값에 IllegalArgumentException 예외 처리를 해주는가?

